Filters are used by reaper.Reaper as a way of filtering and qualifying 
instances for termination. It was a fun way (though probably inefficient)
way to try out some functional style programming in Golang.

